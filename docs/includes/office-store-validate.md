### <a name="office-store-validation"></a>Office validação da Loja

O `validate` comando também faz Office validação da Store, mas permite informações de desenvolvedor como URLs de localhost. Se você quiser executar a validação de nível de produção Office Store, execute o seguinte comando.

```command&nbsp;line
npm run validate -- -p
```